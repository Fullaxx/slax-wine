#!/bin/sh
# Adapted from slax-kitchen @ 0dd1b531624148cf733138a0c0a02f152ab864ff (ci/hooks/pre-push): names this repo and how it is installed here.
# MIT, same author. Do not edit here -- re-copy on a submodule bump; see docs/UPSTREAM.md.
# slax-wine pre-push hook -- installed by the two `ln -sf` lines in docs/build.md.
exec "$(git rev-parse --show-toplevel)/ci/run-checks.sh" pre-push
