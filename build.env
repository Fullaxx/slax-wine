# slax-wine build configuration -- the single source of truth for release identity.
#
# Sourced by build.sh and ci/checks/96-release-consistency.sh. Plain POSIX sh
# assignments: no command substitution, no expansion, so it is safe to source and
# cheap to parse with sed from a gate.
#
# A release is defined by BOTH halves -- our VERSION and the exact base ISO it was
# built on. Changing either is a version bump. The git tag must be v$VERSION; gate 96
# refuses a tagged HEAD where it is not.

VERSION=1.0.0

# ---- the base image ---------------------------------------------------------------
# BASE_TARGET must name a target in vendor/slax-kitchen/compat/sources.yaml, and
# BASE_SHA256/BASE_SIZE must match that entry. Gate 96 cross-checks both, which is what
# catches a submodule bump that silently changes which ISO we build on. `kitchen fetch
# --verify-only` then re-checks the bytes on every build.
#
# 32-bit is not an arbitrary choice: the point of this image is running 32-bit Windows
# binaries under Debian's i386 wine32 + wine32-preloader. A 64-bit base would need
# wine64 plus i386 multiarch -- a larger image and a different project.
BASE_TARGET=debian-32bit-12.2.0
BASE_FLAVOUR=debian
BASE_ARCH=32bit
BASE_VERSION=12.2.0
BASE_ISO=slax-32bit-debian-12.2.0.iso
BASE_SHA256=03b85cd259883f6781b3a3f30ed409b0b6a542b8f510094594c7600bd94e546b
BASE_SIZE=436060160

# ---- the application layer --------------------------------------------------------
# Updating Notepad++ is exactly THREE edits here: APP_URL, APP_SHA256 and APP_VERSION.
# The payload is staged under a stable filename (npp-installer.exe) so nothing outside
# this file has to change -- not the recipe, not the wrapper, not the .desktop entry.
#
# APP_VERSION is not decorative, and leaving it behind is not cosmetic: build.sh writes
# it into /opt/notepadpp/VERSION INSIDE THE ISO (build.sh:87) and into
# build-summary.txt, so a stale value ships an image that misreports its own application
# version. This file used to say "two edits", which is how that would have happened.
# Gate 96 section 2b now cross-checks APP_VERSION against APP_URL, because the URL
# carries the version twice and an instruction nothing enforces is a suggestion.
#
# The NSIS installer rather than the portable tree, deliberately: a portable .exe proves
# only that Wine can load a PE binary, while an installer also exercises the NSIS
# runtime, registry writes, file creation in the prefix and shortcut generation. It is
# PE32/i386, so it runs under wine32; the x64 and arm64 assets would not.
#
# APP_SHA256 is upstream's own published value -- it appears verbatim in
# npp.8.9.8.checksums.sha256, which Notepad++ also signs.
APP_NAME=notepadpp
APP_VERSION=8.9.8
APP_URL=https://github.com/notepad-plus-plus/notepad-plus-plus/releases/download/v8.9.8/npp.8.9.8.Installer.exe
APP_SHA256=9753e2eba8f0ff056d60c52a917f7760ea81227e41b1786260e1ac76db398434
# Read by nothing today -- BASE_SIZE is used twice, and the asymmetry once read as a
# guarantee that did not exist. Kept because `kitchen fetch` verifies size+sha256 for
# the base and this is the same fact for the app: if build.sh ever grows a size check on
# the payload, this is the value it must use. Update it with the other three.
APP_SIZE=6806520

# ---- guard rail -------------------------------------------------------------------
# A runaway apt closure is otherwise invisible until somebody downloads the result.
# Measured 507.3 MiB on the first build; set to that + 5%.
MAX_ISO_MIB=532
